//https://www.codewars.com/kata/54da539698b8a2ad76000228/javascriptfunction isValidWalk(walk) {
  //walk is an array of letters
  //each letter is a direction;
  //you walk a single block for each letter 
  //1 block takes 1 minute
  // create an x + y axis
  //n = 1
  //s = -1
  //w = -1
  //e = 1
  function isValidWalk(walk){
  for(let i = 0;i <= walk.length; i++){
    if(walk.length > 10){
      return false
    }
    else if(walk.length === 10){
      let origin = 0
      if(origin )
      
      return true
    }
  }



}